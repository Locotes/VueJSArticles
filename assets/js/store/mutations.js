export const mutations = {
    setCode(state, input) {
        state.code = input;
    },
    setItems(state, input) {
        state.items = input;
    },
    setBorderRadius(state, input) {
        state.borderRadius = input;
    }
}